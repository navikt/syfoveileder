spring:
  h2.console:
    enabled: true
    path: '/h2'
  datasource:
    url: 'jdbc:h2:mem:testdb'
    driver-class-name: 'org.h2.Driver'
    username: 'sa'
    password:
  jta.atomikos.datasource:
    max-pool-size: 32
    min-pool-size: 8
  cache:
    type: none

server:
  port: 8814
  servlet:
    context-path: /syfoveileder

no.nav.security.oidc:
  issuers: veileder
  issuer:
    veileder:
      discoveryurl: http://metadata
      accepted_audience: aud-localhost
      cookie_name: localhost-idtoken

fasit.environment.name: 'local'

graphApi:
  url: 'https://graph.microsoft.com/v1.0/'
  authority: 'https://login.microsoftonline.com/navq.onmicrosoft.com'

dev: true

aadaccesstoken:
  url: 'aadaccesstoken.url'
aad_syfoveileder_clientid:
  username: "test"
  password: "trustno1"
