spring:
  h2.console:
    enabled: true
    path: '/h2'
  datasource:
    url: 'jdbc:h2:mem:testdb'
    driver-class-name: 'org.h2.Driver'
    username: 'sa'
    password:
  jta.atomikos.datasource:
    max-pool-size: 32
    min-pool-size: 8
  cache:
    type: none

server:
  port: 8814
  servlet:
    context-path: /syfoveileder

securitytokenservice.url: "sts.url"
srvsyfomotebehov:
  username: "username"
  password: "password"

fasit.environment.name: 'local'

dev: true
