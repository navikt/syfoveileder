spring:
  h2.console:
    enabled: true
    path: '/h2'
  datasource:
    url: 'jdbc:h2:mem:testdb'
    driver-class-name: 'org.h2.Driver'
    username: 'sa'
    password:
  jta.atomikos.datasource:
    max-pool-size: 32
    min-pool-size: 8
  cache:
    type: none

server:
  port: 8813
  servlet:
    context-path: /syfoprediksjoner

securitytokenservice.url: "sts.url"
srvsyfomotebehov:
  username: "username"
  password: "password"

no.nav.security.oidc:
  issuers: selvbetjening,intern,veileder
  issuer:
    selvbetjening:
      discoveryurl: http://metadata
      accepted_audience: aud-localhost
      cookie_name: localhost-idtoken
    intern:
      discoveryurl: http://metadata
      accepted_audience: aud-localhost
      cookie_name: localhost-idtoken
    veileder:
      discoveryurl: http://metadata
      accepted_audience: aud-localhost
      cookie_name: localhost-idtoken

fasit.environment.name: 'local'

dev: true
